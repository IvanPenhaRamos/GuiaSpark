Para empezar la app, se debe crear la sesión

```python
spark = SparkSession.builder\
                    .appName('prueba_spark')\
                    .master("local[*]")\
                    .getOrCreate()
SparkContext = spark.sparkContext
sc = SparkContext    #La shell de Spark suele asignar el contexto a la variable sc para abreviar
```

El método `setLogLevel` indica que nos mostrará los logs a partir de Warn (warn y error)

```python
spark.sparkContext.setLogLevel("WARN")
```

A la hora de crear una aplicación conviene *cerrar* la sesión 

```python
spark.stop()
```